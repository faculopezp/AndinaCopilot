# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store
Thumbs.db

# PDFs descargados (se regeneran; no versionar)
*.pdf
downloads/
tmp/

# Excel — solo ignorar temporales de Office (el generado sí se versiona)
~$*.xlsx

# Reporte de cada corrida (se regenera; lo usa el workflow para el email)
run_report.txt
run_report.json

# Planillas crudas del Drive (se bajan a mano; los CSV derivados sí se versionan)
data/_drive_raw/

# Config local de preview (Claude Code)
.claude/
